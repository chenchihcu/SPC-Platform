# Test Fixtures

This directory stores deterministic input fixtures used by regression tests.

Guidelines:
- Keep fixtures small and reproducible.
- Prefer plain text formats (`.csv`, `.json`) when possible.
- Do not store sensitive production data.
