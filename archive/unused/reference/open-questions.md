# Open Questions

未決議事項若影響可驗收行為或契約，決議後請更新對應規格並參考 **`docs/specs/spec_maintenance_and_alignment.md`** §1。

## UI / Theme

- Should the app expose a runtime theme toggle (Apple light only vs light+dark) in a future release?
- Do we want to add screenshot-based visual regression tests for key pages (`chart`, `report`, `data management`) to lock Apple style baseline?
- For DataSetup page, should we enforce a minimum app content width before allowing 3-column mode (beyond current breakpoints) to guarantee Chinese form readability?
