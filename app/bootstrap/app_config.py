APP_NAME = "SPI 製程統計分析"
APP_VERSION = "2.0.0"
# Fallback when QScreen is unavailable; prefer geometry from QScreen.availableGeometry() (spec W-01)
DEFAULT_WINDOW_SIZE = (1280, 720)
# Fraction of available screen to use for initial window.
INITIAL_WINDOW_SCREEN_RATIO = 0.93
